#pragma once

#define BRUFS_VERSION_MAJOR ${BRUFS_VERSION_MAJOR}
#define BRUFS_VERSION_MINOR ${BRUFS_VERSION_MINOR}
#define BRUFS_VERSION_PATCH ${BRUFS_VERSION_PATCH}

#define BRUFS_BUILD_DATE "${BRUFS_BUILD_DATE}"

#define BRUFS_FLAGS "${BRUFS_FLAGS}"

#define BRUFS_GIT_TAG "${GIT_TAG}"
#define BRUFS_GIT_BRANCH "${GIT_BRANCH}"
#define BRUFS_GIT_COMMIT "${GIT_COMMIT}"

#define BRUFS_C_FLAGS "${CMAKE_C_FLAGS}"
#define BRUFS_CXX_FLAGS "${CMAKE_CXX_FLAGS}"

#cmakedefine BRUFS_NANOSECOND_TIMESTAMP
